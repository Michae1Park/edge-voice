"""
Entry point for `edge-voice` console script: parses CLI args, loads
config, and starts the pipeline (and optionally the web UI) as configured.
"""


def main() -> None:
    raise NotImplementedError("TBD - pipeline + webUI")
